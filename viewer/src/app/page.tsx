// Dashboard content is managed by TabPanelManager (always-rendered panel).
// This page intentionally returns null to avoid duplicate DashboardContent in the DOM.
export default function Dashboard() {
  return null;
}
